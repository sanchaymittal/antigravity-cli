# HARD REQUIREMENTS — non-negotiable

1. Before reading any file, query mempalace first: use `mempalace_kg_query` or `mempalace_search`.
2. Before any implementation task, activate the relevant superpowers skill.
3. Save significant findings to mempalace via `mempalace_diary_write` or `mempalace_kg_add`.
4. After completing work, write a session summary to mempalace diary.
5. These rules are not optional. Do not proceed with any task without following them.